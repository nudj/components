const { css } = require('../../css')

const styles = {
  checkbox: {},
  wrapper: {
    position: 'relative'
  },
  root: {
    opacity: 0,
    position: 'absolute'
  }
}

module.exports = css(styles)
