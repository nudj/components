const { StyleSheet } = require('../../css')

const styleSheet = StyleSheet.create({
  inline: {
    paddingTop: 0,
    paddingRight: 0,
    paddingBottom: 0,
    paddingLeft: 0
  }
})

module.exports = styleSheet
